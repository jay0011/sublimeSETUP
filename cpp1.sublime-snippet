#include<bits/stdc++.h>
using namespace std;
#define fastio ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
#define OUT(...) cout << #__VA_ARGS__ << " = " << __VA_ARGS__ << '\n'
#define int long long
#define rep(i,k,n) for(int i=k;i<n;i++)
#define eb emplace_back
#define all(x) x.begin(),x.end()
#define trav(var,arr) for(auto& var : arr)

#define mod 10000007




void solve(){
	$1
}


int32_t main(){
	fastio;
	int tt; cin>>tt;
	while(tt--)
		solve();
}
