[
	{
	"keys": ["alt+`"],
	"command": "toggle_terminus_panel",
	"args": {
		"cwd": "${file_path:${folder}}"
	}
	},

	{
		"keys": ["alt+`"],
		"command": "terminus_close",
		"context": [{"key": "terminus_view"}]
	}
]
